from typing import List

def format_class_attr(classes: List[str]) -> str:
    """
    Formats the classes as a string.

    Args:
        classes (List[str]): The list of classes.

    Returns:
        str: The formatted class attribute.
    """
    return ' '.join(classes)
